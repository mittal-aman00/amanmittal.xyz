{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Blog posts",
  "description": "Links to Medium articles, shown in the Blogs section of amanmittal.xyz.",
  "type": "object",
  "required": ["posts"],
  "properties": {
    "posts": {
      "type": "array",
      "description": "Order does not matter - posts are sorted newest first by date.",
      "items": { "$ref": "#/definitions/post" }
    }
  },
  "definitions": {
    "post": {
      "type": "object",
      "required": ["title", "url", "date", "excerpt"],
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "description": "Article title as it should appear on the card.",
          "minLength": 1
        },
        "url": {
          "type": "string",
          "description": "Full Medium article URL.",
          "format": "uri"
        },
        "date": {
          "type": "string",
          "description": "Publish date in YYYY-MM-DD form.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "excerpt": {
          "type": "string",
          "description": "One or two sentences shown under the title.",
          "minLength": 1
        },
        "tags": {
          "type": "array",
          "description": "Optional topic labels shown as chips.",
          "items": { "type": "string" }
        },
        "readingTime": {
          "type": "string",
          "description": "Optional, e.g. \"6 min read\". Medium shows this on the article page."
        }
      }
    }
  }
}
